export default function Page(){
    return (
        <div>
            <h1 className="h1">
            here we learn about static asset in next js its use adn code and any strucutre not change
            when we build project of static file
            </h1>
        </div>
    )
}